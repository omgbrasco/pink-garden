# Claude Code — Pink Garden / Dumpling

This folder is the whole project. You are on a girlfriend gift: a tiny static iPhone PWA.

**Before any edit, read:**

- `AGENTS.md` (rails — non-negotiable)
- `docs/HANDOFF.md` (what is live right now)
- `docs/REVERSING.md` (how to undo without wrecking cache)

Then make a small change, bump cache v, commit, push `master`. Never force-push. Never add a framework or an LLM.

If a request would post, pay, delete originals, or ship something you have not clicked through on the garden/chat/mic path, stop and ask Braedon.
